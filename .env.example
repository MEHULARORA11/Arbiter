DATABASE_URL=postgres://user:password@localhost:5432/perplexity_clone

# Google OAuth (Phase 2)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
# JWT Session Secret (Short-lived 15-minute session tokens)
JWT_SECRET=

# Credentials Encryption Key (SHA-256 fallback key or any secure string/base64 key used for AES-256-GCM)
CREDENTIALS_ENCRYPTION_KEY=

DB_USER=
DB_PASSWORD=
DB_NAME=
